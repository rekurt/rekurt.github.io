---
import ProjectDetailPage from "../../../views/ProjectDetailPage.astro";
import { productPaths } from "../../../lib/routes";

export function getStaticPaths() { return productPaths("zh-cn"); }
const { locale, product } = Astro.props;
---

<ProjectDetailPage locale={locale} product={product} />
